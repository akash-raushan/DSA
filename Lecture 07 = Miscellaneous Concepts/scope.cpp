#include <bits/stdc++.h>
using namespace std;

int main(){


}



// SCOPE: Area where the variable can be accessed
    // Local: Variable defined in a ifElse, Lopp, Functions or a block of code. these variable can be accessed only inside the block, outside of the block we can not access them.
    // Global: Variables that can be accessed from any where in the code.